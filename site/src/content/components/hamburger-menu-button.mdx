---
title: HamburgerMenuButton
titleJa: ハンバーガーメニューボタン
tier: P2
order: 35
description: "モバイルメニューの開閉トリガー。"
tokenPrefix: hamburger-menu-button
props:
  - { name: open, type: boolean, default: "false", description: "開閉状態" }
  - { name: openLabel, type: string, description: "閉じているときの aria-label" }
  - { name: closeLabel, type: string, description: "開いているときの aria-label" }
---

import Demo from "../../components/Demo.astro";
import DoDont from "../../components/DoDont.astro";
import HamburgerMenuButtonDemo from "../../demos/HamburgerMenuButtonDemo";

## デモ

<Demo>
  <HamburgerMenuButtonDemo client:visible slot="stage" />
  <Fragment slot="code">
    ```tsx
    import { HamburgerMenuButton } from "@soundlabbit/design-system/ui";
    ```
  </Fragment>
</Demo>

## 使い方

Drawer / モバイルナビと組み合わせる。

<DoDont
  dos={["aria-expanded と label を状態に合わせる", "44px 以上のタップ領域を保つ"]}
  donts={["見た目だけのアイコンにする", "デスクトップ常時ナビの代わりに多用する"]}
/>
