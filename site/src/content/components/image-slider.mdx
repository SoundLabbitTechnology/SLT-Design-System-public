---
title: ImageSlider
titleJa: イメージスライダー
tier: P2
order: 51
description: 画像ギャラリー向けカルーセル（Carousel + Image）。
tokenPrefix: ""
props:
 - { name: images, type: "ImageSliderItem[]", description: "src / alt / caption" }
 - { name: "(Carousel props)", type: "—", description: index / autoPlayMs 等を継承 }
---

import Demo from "../../components/Demo.astro";
import DoDont from "../../components/DoDont.astro";
import ImageSliderDemo from "../../demos/ImageSliderDemo";

## デモ

<Demo>
 <ImageSliderDemo client:visible slot="stage" />
 <Fragment slot="code">
 ```tsx
 import { ImageSlider } from "@soundlabbit/design-system/ui";

 <ImageSlider images={[{ src: "...", alt: "説明" }]} />
 ```
 </Fragment>
</Demo>

## 使い方

マーケやギャラリーの画像送り。任意コンテンツは Carousel。

<DoDont
 dos={["各画像に意味のある alt を付ける", "キャプションは必要時のみ"]}
 donts={["装飾だけの画像をスライダーで自動送りする", "テキスト主体のお知らせに使う（Carousel）"]}
/>
